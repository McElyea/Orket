# Current-State Crosswalk
Last updated: 2026-03-27
Status: Accepted for implementation planning
Owner: Orket Core
Lane type: Control-plane foundation / current-state crosswalk

## Purpose

Map current Orket runtime surfaces to the control-plane nouns in this packet.

This document exists to prevent clean-room architecture drift.
It is an honest migration map, not a claim that the current runtime already implements the full control plane.

## Crosswalk

| New control-plane noun | Current surface | Current file or artifact | Status | Migration note | Implementation risk |
| --- | --- | --- | --- | --- | --- |
| `Workload` | Multiple workload-like surfaces still exist across runtime workloads, rocks, and extension entrypoints, but cards, ODR, extension manifests, the main governed run publishers, the manual review-run path, the default sandbox rock path, and the top-level cards epic execution path can now converge onto the core control-plane `WorkloadRecord` family through shared projection and catalog helpers. One universal governed workload authority surface still does not exist yet. | `orket/runtime/workload_adapters.py`; extension manifests under `extensions/`; workload-facing runtime contracts under `docs/specs/WORKLOAD_CONTRACT_V1.md`; `docs/specs/REVIEW_RUN_V0.md`; `orket/core/contracts/workload_identity.py`; `orket/core/contracts/control_plane_models.py`; `orket/application/services/control_plane_workload_catalog.py`; `orket/application/services/cards_epic_control_plane_service.py`; `orket/application/services/review_run_control_plane_service.py`; `orket/application/services/sandbox_control_plane_execution_service.py`; `orket/services/sandbox_orchestrator.py` | conflicting | The implementation lane now has one canonical projection family plus one shared governed-workload catalog for the main run publishers, the manual review-run path, the default sandbox runtime path, and the top-level cards epic execution path, but runtime start paths still need to universalize that family and retire remaining workload-local nouns before this row can move out of `conflicting`. | high |
| `Run` | `ReviewRun` plus run-start artifacts provide the clearest legacy run-like surface, and the manual review-run path, the top-level cards epic execution path, the default sandbox orchestrator path, the authenticated kernel action API path, the default orchestrator issue-dispatch path, the default orchestrator scheduler-owned issue mutation and team-replan child workload composition paths, the governed turn-tool execution path, and the default Gitea worker path now publish supervisor-owned `RunRecord` entries. Governed turn-tool runs now also carry an explicit issue-scoped namespace on the live path. | `orket/application/review/run_service.py`; `orket/application/review/control_plane_projection.py`; `orket/application/review/bundle_validation.py`; `orket/application/review/models.py`; `orket/application/services/review_run_control_plane_service.py`; `orket/application/services/run_ledger_summary_projection.py`; `orket/application/services/microservices_acceptance_reports.py`; `orket/application/services/runtime_policy.py`; `orket/interfaces/api.py`; `orket/interfaces/orket_bundle_cli.py`; `scripts/reviewrun/score_answer_key.py`; `scripts/reviewrun/run_1000_consistency.py`; `scripts/common/run_summary_support.py`; `scripts/probes/probe_support.py`; `scripts/audit/audit_support.py`; `scripts/audit/compare_two_runs.py`; `scripts/training/extract_training_data.py`; `scripts/governance/build_runtime_truth_dashboard_seed.py`; `scripts/acceptance/report_live_acceptance_patterns.py`; `scripts/acceptance/run_monolith_variant_matrix.py`; `scripts/acceptance/check_monolith_readiness_gate.py`; `scripts/acceptance/check_microservices_unlock.py`; `scripts/acceptance/run_architecture_pilot_matrix.py`; `scripts/acceptance/check_microservices_pilot_stability.py`; `scripts/acceptance/decide_microservices_pilot.py`; `scripts/protocol/parity_projection_support.py`; `scripts/protocol/publish_protocol_rollout_artifacts.py`; `scripts/protocol/record_protocol_enforce_window_signoff.py`; `scripts/protocol/run_protocol_enforce_window_capture.py`; `scripts/protocol/check_protocol_enforce_cutover_readiness.py`; `orket/runtime/run_ledger_projection.py`; `orket/runtime/run_ledger_parity.py`; `orket/runtime/protocol_ledger_parity_campaign.py`; `orket/runtime/run_start_artifacts.py`; `orket/runtime/run_summary.py`; `orket/runtime/run_summary_control_plane.py`; `orket/runtime/run_summary_artifact_provenance.py`; `orket/runtime/run_summary_packet2.py`; `orket/core/contracts/control_plane_models.py`; `orket/application/services/cards_epic_control_plane_service.py`; `orket/application/services/sandbox_control_plane_execution_service.py`; `orket/application/services/kernel_action_control_plane_service.py`; `orket/application/services/orchestrator_issue_control_plane_service.py`; `orket/application/services/orchestrator_scheduler_control_plane_service.py`; `orket/application/services/orchestrator_scheduler_control_plane_mutation.py`; `orket/application/services/turn_tool_control_plane_service.py`; `orket/application/services/gitea_state_control_plane_execution_service.py`; `orket/adapters/storage/async_control_plane_execution_repository.py`; `orket/adapters/storage/async_repositories.py`; `orket/services/sandbox_orchestrator.py`; `orket/orchestration/engine.py`; `orket/application/workflows/orchestrator_ops.py`; `orket/application/workflows/turn_executor.py`; `orket/application/workflows/turn_tool_dispatcher.py`; `orket/runtime/execution_pipeline.py`; `orket/runtime/phase_c_runtime_truth.py`; `orket/interfaces/routers/kernel.py`; [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) section 24 | partial | Manual review runs now publish one first-class run per review invocation with durable policy/config snapshots, manifest-visible control-plane refs, fresh review manifests plus persisted review lane decision/critique artifacts now explicitly point execution-state authority at durable control-plane records while marking lane outputs non-authoritative for execution state and now fail closed if those execution-authority markers drift, the embedded review-result `manifest` surface now also validates those persisted execution-authority markers before leaving the process and fails closed if they drift, `orket review replay --run-dir`, direct `--snapshot` plus `--policy` replay when those files target canonical bundle artifacts from the same review-run directory, the review answer-key scoring path, and the review consistency-signature path now also validate those persisted review bundle authority markers before treating bundle artifacts as trustworthy evidence and fail closed if they drift, with replay, scoring, and consistency now also consuming shared validated review-bundle payload or artifact loaders, including the consistency truncation-bounds snapshot path, instead of validating markers and then rereading lane JSON or replay inputs ad hoc, and result or CLI-visible control-plane lifecycle projection now reads durable run refs and state from persisted records instead of collapsing the human surface to state-only text and now fails closed if that review `control_plane` summary stops declaring `projection_only` with source `control_plane_records`, top-level cards epic execution now publishes one invocation-scoped run per `session_id + build_id + invocation timestamp` with durable policy/config snapshots and terminal or waiting lifecycle transitions, cards `run_summary.json` now projects those persisted run or attempt or step records instead of inventing a separate cards-summary run state and now fails closed if that `control_plane` projection stops declaring `projection_only` with source `control_plane_records`, shared probe/workload helpers plus MAR audit completeness, MAR compare, training-data extraction, governance dashboard seed metrics, governance live-proof recorders, and live truthful-runtime proof readers now validate legacy run-summary payloads before trusting them as run evidence, proof inputs, or session-status heuristics, governance dashboard seed metrics now also sanitize persisted `run_ledger.artifact_json` through the shared validated run-ledger projection seam so malformed legacy artifact rows register as invalid-payload signals instead of silently shaping fallback heuristics, live-acceptance pattern reporting now validates persisted `live_acceptance_runs.metrics_json` and `db_summary_json` row payloads before deriving counters or issue-status totals, recording explicit invalid-payload signals instead of silently flattening malformed rows into empty state, monolith variant matrix summaries now preserve those normalized live-report `invalid_payload_signals`, monolith readiness plus matrix-stability gates now fail closed when those matrix summary counts are missing, malformed, or non-zero instead of trusting rate-only matrix summaries derived from malformed live-report rows, architecture pilot matrix comparison now also preserves side-specific invalid-payload totals, detailed per-architecture invalid-payload maps, and failures from the underlying pilot summaries, and microservices pilot stability now fails closed when that persisted comparison detail is missing, malformed, non-zero, or internally inconsistent with its own per-architecture invalid-payload maps instead of trusting architecture delta summaries or stored totals alone, runtime-policy pilot-stability reads now also fail closed when the persisted pilot-stability artifact is structurally malformed or internally inconsistent instead of trusting top-level `stable` / `failures` fields, runtime-policy unlock reads and microservices pilot decision now also fail closed when the persisted unlock artifact is structurally malformed or internally inconsistent, with the shared acceptance-report validator rejecting drift between top-level `unlocked` / `failures` and per-criterion `ok` / `failures` detail instead of trusting top-level unlock state alone, and microservices unlock gating now fails closed when that live report is missing or malformed on `run_count`, `session_status_counts`, `pattern_counters`, or `invalid_payload_signals`, or reports any non-zero invalid source-row counts instead of allowing stale or malformed live-report payloads to produce false-green unlock decisions, API run-detail/session-status surfaces now validate persisted `run_ledger.summary_json` payloads before exposing summary blocks, with run detail also sanitizing the nested `run_ledger.summary_json` projection and both surfaces now sanitizing persisted `run_ledger.artifact_json` through the same validated run-ledger record seam so malformed legacy summary or artifact payloads fail closed to `{}` instead of silently shaping API-visible run state, and protocol/sqlite run-ledger parity, parity-campaign, rollout evidence, enforce-window signoff, enforce-window capture-manifest, and cutover-readiness consumers now also consume that same validated run-ledger projection family while the SQLite run-ledger adapter preserves malformed persisted payload text long enough for parity, API, campaign, rollout-summary, signoff, capture-manifest, and cutover-readiness consumers to detect it, now with rollout/signoff/cutover sharing one invalid-projection detail helper instead of divergent local parsers, so malformed persisted summary or artifact payloads fail closed as explicit parity drift instead of disappearing inside the adapter, being normalized away into false-green comparisons, or being collapsed into generic parity-campaign, rollout-summary, signoff, capture-manifest, or cutover-readiness mismatch counts, fresh receipt-derived artifact provenance entries plus packet-1 provenance and packet-2 source-attribution, narration/effect-audit, and idempotency summaries now preserve canonical governed `control_plane_run_id` refs when authoritative receipt-manifest provenance exists, fresh `run_start_artifacts` now explicitly mark `run_identity` as session-bootstrap `projection_only` evidence instead of silent run authority, and both bootstrap reuse plus legacy run-summary/finalize consumers now fail closed if that run-identity framing drifts, sandbox runtime now has durable run authority for create, reacquire, waiting, and terminal closure paths, the governed kernel action path now publishes one run per `session_id + trace_id`, the default orchestrator issue-dispatch path now publishes one run per `session_id + issue_id + seat + turn_index`, default orchestrator scheduler-owned issue transitions and team-replan child issue creation now publish their own namespace-scoped runs, governed turn tooling now publishes one governed run per `session_id + issue_id + role + turn_index` across both protocol and non-protocol tool turns with an explicit default `issue:<issue_id>` namespace, and the Gitea worker path now publishes one lease-backed run per claimed card and lease epoch, but broader runtime subsystems still split run truth across review-flow objects and observability artifacts. | medium |
| `Attempt` | Retry and lane behavior still dominate much of runtime execution, but the manual review-run path, the top-level cards epic execution path, the default sandbox orchestrator path, the governed kernel action API path, the default orchestrator issue-dispatch path, the default orchestrator scheduler-owned issue mutation and team-replan child workload composition paths, the governed turn-tool execution path, and the default Gitea worker path now publish first-class attempt records with guarded transitions and terminal linkage. | `orket/application/review/lanes/`; `orket/application/review/run_service.py`; `orket/application/services/review_run_control_plane_service.py`; `orket/runtime/retry_classification_policy.py`; `orket/runtime/run_summary_artifact_provenance.py`; `orket/runtime/run_summary_packet2.py`; `orket/core/contracts/control_plane_models.py`; `orket/core/domain/control_plane_lifecycle.py`; `orket/application/services/cards_epic_control_plane_service.py`; `orket/application/services/sandbox_control_plane_execution_service.py`; `orket/application/services/kernel_action_control_plane_service.py`; `orket/application/services/orchestrator_issue_control_plane_service.py`; `orket/application/services/orchestrator_scheduler_control_plane_service.py`; `orket/application/services/orchestrator_scheduler_control_plane_mutation.py`; `orket/application/services/turn_tool_control_plane_service.py`; `orket/application/services/gitea_state_control_plane_execution_service.py`; `orket/adapters/storage/async_control_plane_execution_repository.py`; sandbox restart and recovery services under `orket/application/services/`; `orket/orchestration/engine.py`; `orket/application/workflows/orchestrator_ops.py`; `orket/application/workflows/turn_executor.py`; `orket/application/workflows/turn_tool_dispatcher.py`; `orket/runtime/execution_pipeline.py`; `orket/runtime/phase_c_runtime_truth.py`; `orket/interfaces/routers/kernel.py` | partial | Durable attempt records now exist on the manual review-run path, the top-level cards epic execution path, the main sandbox path, the governed kernel action path, the default orchestrator issue-dispatch path, default orchestrator scheduler-owned issue transitions and team-replan child issue creation, governed turn-tool execution across protocol and non-protocol turns, fresh review lane decision/critique artifacts now preserve canonical review-run `control_plane_attempt_id` refs while explicitly marking execution state non-authoritative, and result or CLI-visible control-plane lifecycle projection now reads durable attempt refs and state from persisted records instead of collapsing the human surface to state-only text, fresh receipt-derived artifact provenance entries plus packet-1 provenance and packet-2 source-attribution, narration/effect-audit, and idempotency summaries now preserve canonical governed `control_plane_attempt_id` refs when authoritative receipt-manifest provenance exists, and the lease-backed Gitea worker path, and fresh retry-classification snapshots now explicitly declare `attempt_history_authoritative=false` so retry policy stops pretending to be hidden attempt truth, but broader runtime retries and other non-sandbox workloads still collapse attempt history into service-local behavior. | medium |
| `Step` | Step-like identity exists in tool invocation contracts and protocol hashing, the manual review-run path now publishes one `review_run_start` `StepRecord`, the top-level cards epic execution path now publishes one invocation-start `StepRecord`, the governed turn-tool execution path now promotes `operation_id` into a first-class `StepRecord` for each executed governed tool operation, the governed kernel action commit path now publishes one first-class step for observed or claimed commit execution, the default orchestrator scheduler-owned issue mutation and team-replan child workload composition paths now publish one namespace-scoped scheduler mutation step per control-plane mutation, and the Gitea worker path now publishes first-class worker-owned claim/finalize transition steps per claimed card execution plus a failed claim step when the initial `ready -> in_progress` mutation is rejected. Governed turn-tool step truth now carries the active issue-scoped namespace and records it among touched resources, governed protocol receipt invocation manifests now also preserve the canonical `control_plane_step_id` alongside the other durable turn-tool refs, reconstructed protocol run-graph tool-call nodes now preserve those canonical step refs when manifest evidence exists, review lane decision and critique artifacts now also preserve the canonical review-run `control_plane_step_id` alongside explicit non-authoritative execution-state markers, receipt-derived artifact provenance entries plus packet-1 provenance and packet-2 source-attribution, narration/effect-audit, and idempotency summaries now preserve canonical governed `control_plane_step_id` refs when authoritative receipt-manifest provenance exists, approval list/detail views can now surface target-side step namespace scope, output ref, resources touched, and receipt refs when the governed target is known, governed kernel replay/audit views can now surface step namespace scope, resources touched, and receipt refs for governed traces, and governed kernel direct API plus async engine responses now surface `control_plane_step_id` when durable step truth exists. | `orket/application/workflows/tool_invocation_contracts.py`; `orket/application/workflows/protocol_hashing.py`; `orket/application/workflows/turn_tool_dispatcher_protocol.py`; `orket/application/review/run_service.py`; `orket/application/services/review_run_control_plane_service.py`; `orket/application/services/cards_epic_control_plane_service.py`; `orket/application/services/turn_tool_control_plane_service.py`; `orket/application/services/kernel_action_control_plane_service.py`; `orket/application/services/kernel_action_control_plane_view_service.py`; `orket/application/services/orchestrator_scheduler_control_plane_service.py`; `orket/application/services/orchestrator_scheduler_control_plane_mutation.py`; `orket/application/services/gitea_state_control_plane_claim_failure_service.py`; `orket/application/services/gitea_state_control_plane_execution_service.py`; `orket/application/services/gitea_state_worker.py`; `orket/adapters/storage/async_control_plane_execution_repository.py`; `orket/application/workflows/turn_tool_dispatcher.py`; `orket/application/workflows/orchestrator_ops.py`; `orket/orchestration/approval_control_plane_read_model.py`; `orket/runtime/execution_pipeline.py`; `orket/runtime/phase_c_runtime_truth.py`; `orket/runtime/protocol_receipt_materializer.py`; `orket/runtime/run_graph_reconstruction.py`; `orket/runtime/run_summary_artifact_provenance.py`; `orket/runtime/run_summary_packet2.py`; `orket/interfaces/routers/kernel.py`; `orket/orchestration/engine.py` | partial | Manual review runs now have one durable start-step hook, top-level cards epic execution now has one durable invocation-start step hook, governed tool execution now has durable step authority on the default execution pipeline path across protocol and non-protocol turns including explicit issue-scoped namespace attribution on the governed turn-tool path, governed turn-tool protocol receipt manifests now preserve canonical `control_plane_step_id` refs instead of leaving step authority only as a bare protocol-local `step_id`, reconstructed protocol run-graph tool-call nodes now preserve those canonical step refs instead of dropping them during graph projection, review lane decision/critique artifacts now preserve canonical review-run `control_plane_step_id` while explicitly marking execution state non-authoritative, and result or CLI-visible control-plane lifecycle projection now reads durable step refs and start-step kind from persisted records instead of collapsing the human surface to state-only text, fresh receipt-derived artifact provenance entries plus packet-1 provenance and packet-2 source-attribution, narration/effect-audit, and idempotency summaries now preserve canonical governed `control_plane_step_id` refs when authoritative receipt-manifest provenance exists, the governed kernel action API path now has one durable step on commit-driven execution truth, default orchestrator scheduler-owned issue transitions and team-replan child issue creation now publish namespace-scoped scheduler mutation steps, approval and kernel read surfaces now expose richer step details when durable truth exists, governed kernel direct API/async engine responses now preserve canonical `control_plane_step_id` refs instead of dropping that step identity on the response contract, and the Gitea worker path now has durable claim, claim-failure, and finalize steps for lease-backed card execution, but broader runtime execution still does not publish one shared step surface. | medium |
| `Effect` | Receipts, runtime artifacts, and run summary surfaces still capture parts of effect truth, and the default sandbox orchestrator path, the governed kernel action commit path, the default orchestrator issue-dispatch path, the default orchestrator scheduler-owned issue mutation and team-replan child workload composition paths, the governed turn-tool execution path, and the Gitea worker path now publish first-class effect journal entries. Sandbox operator views now expose the latest effect intended target, observed result ref, and uncertainty classification, approval list/detail views can now surface target-side effect intended target, observed result ref, authorization-basis ref, and uncertainty when the governed target is known, governed kernel replay/audit views now preserve the same durable effect counts alongside richer step and final-truth detail, legacy protocol receipt materialization now explicitly marks materialized run-ledger events as projection-only with an explicit source and projected governed turn-tool effect-journal refs instead of silently replaying receipt evidence as native effect authority, and governance live-proof recorders plus live truthful-runtime proof tests now consume one shared validated legacy run-summary loader before reading packet-1, packet-2 repair, or artifact-provenance summary blocks. | `workspace/observability/<run_id>/`; `orket/runtime/protocol_receipt_materializer.py`; `orket/runtime/run_summary.py`; `orket/core/contracts/control_plane_effect_journal_models.py`; `orket/core/domain/control_plane_effect_journal.py`; `orket/application/services/control_plane_publication_service.py`; `orket/application/services/sandbox_control_plane_effect_service.py`; `orket/application/services/sandbox_lifecycle_view_service.py`; `orket/application/services/kernel_action_control_plane_service.py`; `orket/application/services/kernel_action_control_plane_view_service.py`; `orket/application/services/orchestrator_issue_control_plane_service.py`; `orket/application/services/orchestrator_scheduler_control_plane_service.py`; `orket/application/services/orchestrator_scheduler_control_plane_mutation.py`; `orket/application/services/turn_tool_control_plane_service.py`; `orket/application/services/gitea_state_control_plane_execution_service.py`; `orket/application/services/gitea_state_worker.py`; `orket/orchestration/approval_control_plane_read_model.py`; `orket/adapters/storage/async_control_plane_record_repository.py`; `orket/services/sandbox_orchestrator.py`; `orket/orchestration/engine.py`; `orket/application/workflows/orchestrator_ops.py`; `orket/application/workflows/turn_tool_dispatcher.py`; `orket/kernel/v1/nervous_system_runtime.py`; `orket/runtime/execution_pipeline.py`; `scripts/common/run_summary_support.py`; `scripts/governance/record_truthful_runtime_packet1_live_proof.py`; `scripts/governance/record_truthful_runtime_packet2_repair_live_proof.py`; `scripts/governance/record_truthful_runtime_artifact_provenance_live_proof.py`; `tests/live/run_summary_support.py` | partial | The effect journal is now live for sandbox deploy, verified cleanup, committed governed kernel actions with execution-result evidence, policy-rejected governed kernel actions when execution was actually observed, default orchestrator issue-dispatch start transitions, default orchestrator scheduler-owned issue transitions and team-replan child issue creation, governed turn-tool operations across protocol and non-protocol turns, and Gitea worker claim/finalize state transitions on the non-sandbox state-backend path, while approval, kernel, and sandbox read surfaces can now expose richer effect detail when durable truth exists, legacy protocol receipt materialization now explicitly marks run-ledger `tool_call` / `operation_result` rows as `projection_only` with source `observability.protocol_receipts.log` while projecting governed turn-tool effect-journal ids rather than silently presenting receipt replay as native effect authority, legacy run-summary packet-1, packet-2, and artifact-provenance blocks now self-identify as fact-backed `projection_only` surfaces and fail closed when that framing drifts instead of reading like alternate effect or closure authority, and governance live-proof recorders plus live truthful-runtime proof readers now also consume one shared validated legacy run-summary loader before trusting packet-1, packet-2 repair-ledger, or artifact-provenance summary blocks. Broader workload execution and checkpoint-coupled effect publication still do not flow through the journal by default. | high |
| `Resource` | First-class `ResourceRecord` contracts now exist in core, and the default sandbox lifecycle, sandbox create-record failure rollback, standalone coordinator claim path plus promotion-failure rollback, Gitea state-worker lease lifecycle, governed kernel-action execution authority, governed turn-tool namespace authority, default orchestrator issue-dispatch slot, and default orchestrator scheduler-owned namespace mutation paths now publish durable resource snapshots through that shared record family. Sandbox operator views, the standalone coordinator API, governed kernel replay or audit views, authenticated kernel admit/commit/session-end responses, async orchestration-engine kernel admit/commit/end-session responses, approval target views for supported governed runs, supported approval-resolution operator-action summaries, governed kernel replay/audit operator-action summaries, authenticated sandbox cancel operator-action summaries, and governed turn-tool protocol receipt invocation manifests can surface or reference the latest resource id, kind, current state, and orphan classification when durable truth exists, orchestrator issue-dispatch, scheduler, governed turn-tool, governed kernel existing-run guards, standalone coordinator renew/expiry/release paths, and the Gitea worker active renew path now also validate that latest resource truth against their lease authority, and Gitea worker terminal expiry/release helpers now realign resource truth instead of skipping on lease-only terminal history. | `orket/core/contracts/control_plane_models.py`; `orket/application/services/control_plane_authority_service.py`; `orket/application/services/control_plane_publication_service.py`; `orket/application/services/control_plane_resource_authority_checks.py`; `orket/application/services/control_plane_target_resource_refs.py`; `orket/application/services/sandbox_control_plane_operator_service.py`; `orket/application/services/sandbox_control_plane_resource_service.py`; `orket/application/services/sandbox_runtime_lifecycle_service.py`; `orket/application/services/sandbox_lifecycle_reconciliation_service.py`; `orket/application/services/sandbox_terminal_outcome_service.py`; `orket/application/services/sandbox_runtime_cleanup_service.py`; `orket/application/services/sandbox_runtime_inspection_service.py`; `orket/application/services/sandbox_lifecycle_view_service.py`; `orket/services/sandbox_orchestrator.py`; `orket/application/services/coordinator_control_plane_lease_service.py`; `orket/application/services/coordinator_control_plane_reservation_service.py`; `orket/application/services/gitea_state_control_plane_lease_service.py`; `orket/application/services/gitea_state_control_plane_reservation_service.py`; `orket/application/services/gitea_state_worker.py`; `orket/application/services/kernel_action_control_plane_operator_service.py`; `orket/application/services/kernel_action_control_plane_resource_lifecycle.py`; `orket/application/services/kernel_action_control_plane_service.py`; `orket/application/services/kernel_action_control_plane_view_service.py`; `orket/application/services/kernel_action_pending_approval_reservation.py`; `orket/application/services/turn_tool_control_plane_resource_lifecycle.py`; `orket/application/services/turn_tool_control_plane_state_gate.py`; `orket/application/services/tool_approval_control_plane_operator_service.py`; `orket/application/services/orchestrator_issue_control_plane_service.py`; `orket/application/services/orchestrator_scheduler_control_plane_service.py`; `orket/application/services/orchestrator_scheduler_control_plane_mutation.py`; `orket/application/workflows/tool_invocation_contracts.py`; `orket/application/workflows/turn_tool_dispatcher_protocol.py`; `orket/interfaces/coordinator_api.py`; `orket/interfaces/routers/kernel.py`; `orket/orchestration/engine.py`; `orket/orchestration/approval_control_plane_read_model.py`; `orket/orchestration/engine_approvals.py`; `orket/adapters/storage/async_control_plane_record_repository.py` | partial | The shared resource-model seam now exists in code and now publishes durable resource truth across sandbox plus the main governed non-sandbox ownership paths, including fail-closed coordinator promotion rollback and sandbox create-record failure rollback, the coordinator API, governed kernel replay or audit views, authenticated kernel admit/commit/session-end responses, async engine kernel admit/commit/end-session responses, approval target views for supported governed runs, supported approval-resolution operator-action summaries, governed kernel replay/audit operator-action summaries, authenticated sandbox cancel operator-action summaries, and governed turn-tool protocol receipt invocation manifests now project or reference that resource truth directly instead of remaining lease-only, run-ref-only, receipt-only, or subsystem-local. Approval target views and supported approval-resolution operator-action summaries now also project canonical shared resource ids for default orchestrator issue-dispatch runs plus scheduler-owned namespace mutation and child-workload composition runs when durable execution truth exists, instead of limiting that resource projection to governed turn-tool and kernel-action targets. Orchestrator issue-dispatch, scheduler, governed turn-tool, governed kernel existing-run guards, standalone coordinator renew/expiry/release authority, and Gitea worker active renew now fail closed when resource truth drifts from lease truth, and the coordinator open-cards observation plus claim path now check stale expiry authority before mutating store ownership. Gitea worker terminal expiry/release helpers now realign resource truth when it drifts away from already-terminal lease truth. Broader read surfaces and any still-uncovered authority paths still do not consume one universal resource registry. | medium |
| `Reservation` | First-class reservation contracts and reservation-to-lease progression rules now exist in core, the default sandbox orchestrator path now publishes an allocation reservation before lifecycle create and promotes it explicitly when lease authority begins, approval-gated admission now publishes a first-class operator-hold reservation for repo-backed tool-approval requests, guard-review pending-gate requests, and governed kernel approvals, with async orchestration-engine kernel admit now owning the governed-kernel approval-hold publication before response shaping, the default orchestrator issue-dispatch path now publishes a first-class concurrency reservation before promoting it into lease authority for the active issue dispatch slot, default orchestrator scheduler-owned issue transitions and team-replan child issue creation now publish first-class issue-scoped namespace reservations before promoting them into lease authority for those scheduler and child-workload mutations, the governed turn-tool execution path now publishes an issue-scoped namespace reservation at governed admission and keeps checkpoint acceptance explicitly dependent on that reservation, the standalone coordinator API now publishes a first-class non-hedged claim reservation before promoting it into lease authority on successful claim, the governed kernel replay and audit views now surface the latest run-scoped reservation summary when durable truth exists, approval list/detail views now surface richer target-side reservation summaries including reservation kind, invalidation basis, supervisor authority, and promotion linkage when the governed target is known, and the default Gitea state worker path now publishes a first-class claim reservation after backend lease acquire, promotes it only after the initial `ready -> in_progress` claim transition succeeds, and invalidates it on claim-stage failure. | `orket/core/contracts/control_plane_models.py`; `orket/core/domain/control_plane_reservations.py`; `orket/application/services/sandbox_control_plane_reservation_service.py`; `orket/application/services/tool_approval_control_plane_reservation_service.py`; `orket/application/services/kernel_action_pending_approval_reservation.py`; `orket/application/services/coordinator_control_plane_reservation_service.py`; `orket/application/services/kernel_action_control_plane_view_service.py`; `orket/application/services/orchestrator_issue_control_plane_service.py`; `orket/application/services/orchestrator_scheduler_control_plane_service.py`; `orket/application/services/orchestrator_scheduler_control_plane_mutation.py`; `orket/application/services/gitea_state_control_plane_reservation_service.py`; `orket/application/services/turn_tool_control_plane_resource_lifecycle.py`; `orket/application/services/control_plane_publication_service.py`; `orket/orchestration/engine.py`; `orket/orchestration/approval_control_plane_read_model.py`; `orket/adapters/storage/async_control_plane_record_repository.py`; `orket/services/sandbox_orchestrator.py`; `orket/application/services/sandbox_runtime_lifecycle_service.py`; `orket/application/workflows/orchestrator_ops.py`; `orket/application/workflows/turn_executor_control_plane.py`; `orket/interfaces/routers/kernel.py`; `orket/interfaces/coordinator_api.py`; `orket/orchestration/engine_approvals.py`; `orket/application/services/gitea_state_worker.py`; `orket/runtime/execution_pipeline.py` | partial | Reservation truth is now live on the main sandbox path, on approval-gated operator-hold paths for repo-backed tool approvals, guard-review pending gates, and governed kernel approvals, with the async engine admit path now owning kernel approval-hold publication before response shaping, on the default orchestrator issue-dispatch path for active issue scheduling, on default orchestrator scheduler-owned issue transitions and team-replan child issue composition for issue-scoped namespace authority, on the governed turn-tool admission path for issue-scoped namespace authority with checkpoint dependency linkage, on the standalone coordinator claim path for non-hedged cards, and on the Gitea worker claim path for lease-backed card execution, while the main governed reservation-to-lease paths now sit beside shared resource publication rather than lease-only ownership stories, governed kernel replay and audit views can now surface the latest reservation summary for the run, and approval list/detail views can surface richer target-side reservation summaries when the governed target is known, but broader admission, scheduling, and other non-sandbox runtime paths still do not publish or enforce one shared reservation authority family. | medium |
| `Lease` | Lease-like semantics already exist in state backend and coordinator paths, sandbox lifecycle now publishes first-class lease snapshots on the default runtime path, the default orchestrator issue-dispatch path now promotes its concurrency reservation into an active issue dispatch lease and releases that lease on closeout, default orchestrator scheduler-owned issue transitions and team-replan child issue creation now promote issue-scoped namespace reservations into active namespace leases and release those leases on closeout, the governed turn-tool execution path now promotes its issue-scoped namespace reservation into an active execution lease and releases that lease explicitly on governed closeout, the Gitea state worker path now publishes first-class lease history for non-sandbox card processing with explicit linkage back to claim reservations, and the standalone coordinator API now publishes first-class non-hedged lease history for claim, renew, expiry-before-reclaim, open-cards expiry observation, and release transitions. Coordinator responses now surface lease resource id, expiry basis, granted timestamp, publication timestamp, and last confirmed observation alongside the latest lease summary. | `orket/core/contracts/control_plane_models.py`; `orket/core/contracts/state_backend.py`; `orket/core/domain/coordinator_card.py`; `orket/application/services/coordinator_control_plane_lease_service.py`; `orket/application/services/coordinator_control_plane_reservation_service.py`; `orket/interfaces/coordinator_api.py`; `orket/application/services/sandbox_control_plane_lease_service.py`; `orket/application/services/sandbox_control_plane_resource_service.py`; `orket/application/services/orchestrator_issue_control_plane_service.py`; `orket/application/services/orchestrator_scheduler_control_plane_service.py`; `orket/application/services/orchestrator_scheduler_control_plane_mutation.py`; `orket/application/services/turn_tool_control_plane_resource_lifecycle.py`; `orket/application/services/gitea_state_control_plane_claim_failure_service.py`; `orket/application/services/gitea_state_control_plane_lease_service.py`; `orket/application/services/gitea_state_control_plane_reservation_service.py`; `orket/application/services/gitea_state_worker.py`; sandbox cleanup, reconciliation, and lifecycle services under `orket/application/services/`; `orket/adapters/storage/async_control_plane_record_repository.py`; `orket/services/sandbox_orchestrator.py`; `orket/application/workflows/turn_executor_control_plane.py`; `orket/runtime/execution_pipeline.py` | partial | Sandbox runtime can now publish pending, active, expired, uncertain, and released lease truth across create, health-verified activation, renew, reclaim, terminal, and cleaned paths, the default orchestrator issue-dispatch path now publishes an active issue-dispatch lease with explicit source-reservation linkage and closeout release, default orchestrator scheduler-owned issue transitions and team-replan child issue creation now publish active and released issue-scoped namespace lease history, the governed turn-tool path now publishes an active issue-scoped namespace lease during governed execution with explicit source-reservation linkage and closeout release, the Gitea worker path now publishes active, renewed, expired, released, and claim-failure-driven uncertain lease history for non-sandbox card execution with explicit source-reservation linkage, and the standalone coordinator API now publishes non-hedged claim, renew, expiry-before-reclaim, open-cards expiry observation, and release history linked to explicit claim reservations while surfacing lease resource id, expiry basis, granted timestamp, publication timestamp, and last confirmed observation on the response path; those same major paths now also mirror shared resource publication alongside lease transitions, but admission, scheduling, and other subsystems still do not consume one shared lease authority family. | medium |
| `Checkpoint` | `ReviewSnapshot` and snapshot loading remain useful existing seams, the default sandbox reclaimable path now publishes first-class checkpoint and checkpoint-acceptance records backed by immutable lifecycle snapshots with `resume_new_attempt_from_checkpoint` semantics, the governed turn execution path now publishes a durable pre-effect `resume_same_attempt` checkpoint plus acceptance backed by an immutable checkpoint snapshot artifact before tool execution begins, now carries the governed namespace reservation and execution lease refs as explicit checkpoint-acceptance dependencies, consumes that accepted immutable snapshot before prompt/model work on safe pre-effect `resume_mode`, keeps continuation on the current attempt instead of bootstrapping attempt 2, now fails closed when snapshot identity drifts from the current run or namespace request, still consumes older `resume_new_attempt_from_checkpoint` governed checkpoint lineage when those records already exist, and the Gitea worker path now publishes a durable pre-effect `resume_forbidden` checkpoint plus acceptance from the claimed-card lease observation before any worker-owned state mutation. | `orket/application/review/snapshot_loader.py`; `orket/application/review/models.py`; `orket/core/contracts/control_plane_models.py`; `orket/core/contracts/control_plane_effect_journal_models.py`; `orket/core/domain/control_plane_effect_journal.py`; `orket/application/services/control_plane_publication_service.py`; `orket/application/services/sandbox_control_plane_checkpoint_service.py`; `orket/application/services/gitea_state_control_plane_checkpoint_service.py`; `orket/application/services/gitea_state_worker.py`; `orket/application/workflows/turn_executor_control_plane.py`; `orket/application/workflows/turn_executor_resume_replay.py`; `orket/application/workflows/turn_executor_completed_replay.py`; `orket/application/workflows/turn_executor_control_plane_evidence.py`; `orket/application/services/turn_tool_control_plane_resource_lifecycle.py`; `orket/adapters/storage/async_control_plane_record_repository.py`; `orket/adapters/storage/async_sandbox_lifecycle_repository.py`; `orket/runtime/execution_pipeline.py`; [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) section 24 | partial | Sandbox lease-expiry reconciliation can now publish a durable checkpoint plus acceptance tied to an immutable lifecycle snapshot with `resume_new_attempt_from_checkpoint` semantics, governed turn execution now publishes a durable pre-effect `resume_same_attempt` checkpoint plus acceptance backed by an immutable checkpoint snapshot artifact with explicit reservation/lease dependency refs, consumes that accepted immutable snapshot for unfinished pre-effect same-attempt recovery on `resume_mode`, now fails closed when that immutable snapshot no longer matches the current run or namespace request, still consumes older governed `resume_new_attempt_from_checkpoint` lineage truthfully when those records already exist, and the Gitea worker path now publishes a durable pre-effect `resume_forbidden` checkpoint plus acceptance from the claimed-card observation before non-sandbox worker mutation begins, but broader supervisor-owned checkpoint creation still is not wired through execution flows. | medium |
| `RecoveryDecision` | Sandbox recovery and restart policy services still own much live recovery behavior, the default sandbox orchestrator path now publishes durable recovery decisions for checkpoint-backed new-attempt reacquire and terminal-stop cases, the governed turn execution path now publishes checkpoint-backed same-attempt recovery decisions for unfinished pre-effect `resume_mode` recovery on the current attempt, still consumes older governed checkpoint-backed new-attempt recovery lineage when those records already exist, publishes reconciliation-required recovery decisions plus a second reconciliation-rationalized terminal `terminate_run` decision when `resume_mode` encounters unfinished post-effect or effect-boundary-uncertain governed execution, and now applies that same immediate reconciliation-closed terminal stop when a same-attempt or replacement-attempt resume already carries step or effect truth instead of treating it as a clean retry, and terminal `terminate_run` recovery decisions with explicit checkpoint/effect preconditions and blocked continuation actions for blocked pre-effect and failed post-effect governed execution, and the Gitea worker path now publishes terminal recovery decisions for lease-expiry, runtime-failure, and claim-transition-failure closure on claimed non-sandbox card execution. | `orket/application/services/sandbox_runtime_recovery_service.py`; `orket/application/services/sandbox_restart_policy_service.py`; `orket/application/services/sandbox_control_plane_execution_service.py`; `orket/application/services/turn_tool_control_plane_closeout.py`; `orket/application/services/turn_tool_control_plane_reconciliation.py`; `orket/application/services/turn_tool_control_plane_service.py`; `orket/application/services/turn_tool_control_plane_recovery.py`; `orket/application/services/gitea_state_control_plane_claim_failure_service.py`; `orket/application/services/gitea_state_control_plane_execution_service.py`; `orket/application/services/gitea_state_worker.py`; `orket/application/services/sandbox_lifecycle_view_service.py`; `orket/core/contracts/control_plane_models.py`; `orket/core/domain/control_plane_recovery.py`; `orket/application/services/control_plane_publication_service.py`; `orket/adapters/storage/async_control_plane_record_repository.py`; `orket/runtime/execution_pipeline.py` | partial | Recovery authority is now durable on key sandbox runtime paths, the sandbox reclaimable path now publishes checkpoint-backed new-attempt recovery decisions that target the accepted checkpoint directly after lease-expiry reconciliation, the governed turn path now publishes checkpoint-backed same-attempt recovery decisions when `resume_mode` recovers an unfinished pre-effect governed attempt on the current attempt while still consuming older governed new-attempt lineage truthfully when it exists, and immediately closes unsafe same-attempt or replacement-attempt resumes once step or effect truth already exists, explicit reconciliation-record publication plus `require_reconciliation_then_decide` recovery authority and an immediate reconciliation-rationalized terminal `terminate_run` closeout when governed turn `resume_mode` encounters unfinished post-effect or effect-boundary-uncertain execution, terminal `terminate_run` decisions with checkpoint/effect preconditions and blocked continuation actions for blocked pre-effect and failed post-effect governed execution, the Gitea worker path now publishes terminal `terminate_run` decisions for non-sandbox lease-expiry, claim-transition-failure, and runtime-failure closure, and sandbox operator views preserve the latest recovery decision and checkpoint summary across attempt rollover after reacquire, but broader restart policy and other non-sandbox runtimes still do not publish one shared decision surface. | medium |
| `ReconciliationRecord` | Sandbox reconciliation remains the strongest existing subsystem-specific surface, and a first-class reconciliation publication path now exists for the sandbox `lost_runtime` closure case, unsafe governed turn `resume_mode` closeout on the default execution path including orphan operation-artifact detection before prompt/model work, and Gitea worker claim-transition failure on the non-sandbox state-backend path. Sandbox operator views on the default orchestrator path now also surface the latest reconciliation id, divergence class, and safe-continuation class when durable control-plane truth exists for the run. | `orket/application/services/sandbox_lifecycle_reconciliation_service.py`; `orket/application/services/sandbox_control_plane_reconciliation_service.py`; `orket/application/services/turn_tool_control_plane_reconciliation.py`; `orket/application/services/turn_tool_control_plane_recovery.py`; `orket/application/services/gitea_state_control_plane_claim_failure_service.py`; `orket/application/services/control_plane_publication_service.py`; `orket/application/services/sandbox_lifecycle_view_service.py`; `orket/adapters/storage/async_control_plane_record_repository.py`; `orket/core/contracts/control_plane_models.py`; `orket/application/workflows/turn_executor_control_plane_evidence.py`; `orket/application/workflows/turn_executor_resume_replay.py` | partial | Reconciliation authority is now durable for the sandbox `lost_runtime` closure case, unsafe governed turn `resume_mode` closeout including durable orphan operation-artifact detection before continuation, and Gitea worker claim-transition failure, and the default sandbox operator view can now surface the latest reconciliation summary when it exists, but broader divergence classes and continuation paths still need to publish the same record family. | medium |
| `OperatorAction` | Operator surfaces exist in APIs and operator-policy artifacts, and the authenticated sandbox stop path now publishes a first-class `OperatorActionRecord` for explicit cancel commands while authenticated session halt now publishes a first-class cancel command on a session-scoped target and authenticated interaction cancel now publishes first-class cancel commands on interaction session or turn targets. Authenticated approval resolutions now publish first-class operator records for both risk acceptance on supported tool-approval grants and terminal commands on denials or expirations. Supported guard-review pending-gate resolutions now also publish first-class operator commands on the pending-gate surface, and when a supported governed tool-approval target can be resolved to durable execution truth, those approval decisions now also carry the canonical shared target `resource_id` in `affected_resource_refs` and publish a second operator action on the governed target run. Governed kernel approvals can now recover that governed target from authoritative approval-reservation truth even when the nervous-system approval payload omits `control_plane_target_ref`, and the authenticated kernel session-end path now also publishes a first-class cancel command when it terminally cancels a governed kernel-action run plus an optional bounded `operator_attestation` action when explicitly requested, both now carrying the canonical execution-scope resource id in durable `affected_resource_refs`. Authenticated sandbox stop now also carries the canonical shared sandbox `resource_id` in durable `affected_resource_refs`, and sandbox operator views now expose affected transition/resource refs on the latest operator-action summary instead of truncating that view to receipt-only evidence. Approval list and detail surfaces now expose both the approval-surface operator-action summary and the latest target-side run and attempt summary when durable execution truth exists, plus the latest target-side step summary, checkpoint summary, effect-journal summary, operator action summary including `input_class`, `command_class`, `risk_acceptance_scope`, `attestation_scope`, `attestation_payload`, `precondition_basis_ref`, receipt refs, and affected transition/resource refs, reservation summary, and final-truth summary when the payload or reservation truth names a governed target, while governed kernel replay/audit views now expose affected transition/resource refs on the latest operator-action summary instead of truncating that view to receipt-only evidence. | `orket/interfaces/api.py`; `orket/interfaces/routers/sessions.py`; `orket/interfaces/routers/approvals.py`; `orket/interfaces/routers/kernel.py`; `orket/orchestration/engine.py`; `orket/orchestration/engine_services.py`; `orket/orchestration/engine_approvals.py`; `orket/orchestration/approval_control_plane_read_model.py`; `orket/application/services/control_plane_target_resource_refs.py`; `orket/application/services/pending_gate_control_plane_operator_service.py`; `orket/application/services/sandbox_control_plane_operator_service.py`; `orket/application/services/sandbox_lifecycle_view_service.py`; `orket/application/services/tool_approval_control_plane_operator_service.py`; `orket/application/services/kernel_action_control_plane_operator_service.py`; `orket/application/services/kernel_action_control_plane_view_service.py`; `orket/application/services/control_plane_publication_service.py`; `orket/application/workflows/orchestrator.py`; `orket/application/workflows/orchestrator_ops.py`; `orket/adapters/storage/async_control_plane_record_repository.py`; `orket/runtime/operator_override_logging_policy.py` | partial | Explicit authenticated sandbox stop now records a durable operator command with the canonical shared sandbox resource id in `affected_resource_refs`, authenticated session halt now records a durable session-scoped `cancel_run` operator command, authenticated interaction cancel now records durable session-scoped or turn-scoped `cancel_run` operator commands, authenticated tool-approval grants now record durable operator risk acceptance, supported tool-approval decisions for governed turn-tool and kernel-action targets now carry the canonical shared target resource id in `affected_resource_refs` when durable execution truth exists, supported guard-review pending-gate approvals now record durable `approve_continue` commands on the pending-gate target, approval denials and expirations now record durable terminal operator commands on the approval target and on governed turn-tool runs when the approval payload or approval reservation names that target explicitly, authenticated kernel session-end cancel plus bounded attestation now record durable operator actions for governed action runs with canonical execution-scope resource ids in `affected_resource_refs`, and approval/kernel/sandbox read surfaces now preserve operator input split detail explicitly including affected transition/resource refs on the latest sandbox and governed-kernel operator summaries, but broader non-sandbox operator surfaces still are not published through one shared authority family. | medium |
| `FinalTruthRecord` | Run summary and runtime truth contracts still provide legacy closure truth, and a first-class control-plane final-truth record family now exists with partial sandbox workflow, policy, and lifecycle terminal-outcome integration plus `lost_runtime` reconciliation on the default orchestrator path, commit/session-end closure on the governed kernel action API path, governed turn-tool closure on the default execution path including reconciliation-closed unsafe `resume_mode` termination, and lease-backed worker closure on the default Gitea state-backend path. Sandbox operator views now surface the fuller final-truth classifications already published for the run, including terminality basis, authoritative result ref, and authority sources, and governed kernel replay/audit views now surface fuller final-truth classifications plus authoritative result ref and authority sources for governed traces. | `orket/runtime/run_summary.py`; `orket/runtime/runtime_truth_contracts.py`; `docs/specs/TRUTHFUL_RUNTIME_PACKET1_CONTRACT.md`; `orket/core/contracts/control_plane_models.py`; `orket/core/domain/control_plane_final_truth.py`; `orket/application/services/control_plane_publication_service.py`; `orket/application/services/sandbox_control_plane_closure_service.py`; `orket/application/services/sandbox_control_plane_reconciliation_service.py`; `orket/application/services/kernel_action_control_plane_service.py`; `orket/application/services/kernel_action_control_plane_view_service.py`; `orket/application/services/turn_tool_control_plane_service.py`; `orket/application/services/gitea_state_control_plane_claim_failure_service.py`; `orket/application/services/gitea_state_control_plane_execution_service.py`; `orket/application/services/gitea_state_worker.py`; `orket/application/services/sandbox_terminal_outcome_service.py`; `orket/application/services/sandbox_lifecycle_reconciliation_service.py`; `orket/application/services/sandbox_lifecycle_view_service.py`; `orket/adapters/storage/async_control_plane_record_repository.py`; `orket/services/sandbox_orchestrator.py`; `orket/orchestration/engine.py`; `orket/application/workflows/orchestrator_ops.py`; `orket/application/workflows/turn_tool_dispatcher.py`; `orket/runtime/execution_pipeline.py`; `orket/interfaces/routers/kernel.py` | partial | Final-truth authority now exists and sandbox workflow, policy, and lifecycle terminal outcomes plus `lost_runtime` reconciliation can publish it on the main sandbox runtime path, the governed kernel action API path now publishes terminal closure for admit/commit/end-session traces, governed turn tooling now publishes terminal closure for governed tool execution across protocol and non-protocol turns including immediate reconciliation-closed termination when unsafe `resume_mode` encounters post-effect or effect-boundary-uncertain truth, sandbox operator views now surface result, closure basis, terminality basis, evidence sufficiency, residual uncertainty, degradation, authoritative result ref, and authority sources when that truth exists, governed kernel replay/audit views now surface the same richer final-truth classifications including authoritative result ref and authority sources, and the Gitea worker path now publishes terminal closure for lease-backed non-sandbox card execution including blocked pre-effect claim-transition failure, but other runtime closure paths still need to adopt the same authority surface. | high |

## Current-state conclusion

The strongest current alignment exists around:
1. run identity and run-start artifacts
2. sandbox execution, resource, reservation, reconciliation, lease, operator-command, and final-truth publication services on the default orchestrator path
3. sandbox operator views now expose control-plane run state, attempt state, reservation and lease status, latest resource summary, final-truth summary, latest operator command summary including affected transition/resource refs, and the latest reconciliation id, divergence class, and safe-continuation class when durable reconciliation truth exists
4. repo-backed tool-approval requests, guard-review pending gates, and governed kernel approvals now publish explicit operator-hold reservation truth on request creation or admission and resolve that reservation on approval decision, with approval list and detail surfaces exposing the latest reservation summary on both the approval surface and the named governed target when present, plus the latest target-side run and attempt summary when durable execution truth exists including policy/config snapshot ids, creation timestamp, and attempt count, the latest target-side resource summary for supported governed runs, the latest target-side step, checkpoint, and effect-journal summary when durable target truth exists including step capability, checkpoint acceptance supervisor or dependency detail, and effect publication lineage or digest detail, richer target-side operator summaries including receipt refs and affected transition/resource refs plus canonical target resource ids for supported governed targets, the latest target-side final-truth summary when durable closure truth exists, and governed kernel approvals can now recover that target from reservation truth instead of relying on a copied payload field
5. authenticated tool-approval resolutions now publish explicit operator records on the default API-to-engine approval path, with grants recorded as risk acceptance and denials or expirations recorded as terminal commands
6. authenticated session halt now publishes explicit session-scoped operator `cancel_run` commands, authenticated interaction cancel now publishes explicit session-scoped or turn-scoped operator `cancel_run` commands, and authenticated kernel session-end now publishes explicit operator cancel commands for governed kernel-action runs on the default API path and can publish an explicit bounded `operator_attestation` record for the same run when requested
7. the governed kernel action API path now publishes per-trace run, attempt, commit-step, effect-journal, and final-truth authority for admit, commit, and session-end closure, its execution-lease lifecycle now also publishes shared `ResourceRecord` snapshots for execution-scope activation, terminal release, and fail-closed activation rollback, existing-run consistency now fails closed when durable resource truth drifts from the lease, governed kernel approval-hold reservation publication on the async engine admit path is now application-owned instead of router-local, and authenticated kernel admit/commit/session-end responses plus the async engine admit/commit/end-session methods now surface `control_plane_resource_id` whenever that durable resource truth exists
8. kernel replay and audit summaries now surface latest step identity plus namespace scope, resources touched, and receipt refs, the latest resource summary, latest reservation summary including invalidation basis and supervisor authority, and closure state alongside run, attempt, effect, final-truth, and operator-action summaries, while approval-derived views on governed kernel targets can also expose policy/config snapshot ids, creation timestamp, attempt count, and effect publication detail through the same durable records
9. the sandbox reclaimable path now publishes checkpoint-backed new-attempt recovery decisions that target the accepted reclaimable checkpoint directly, with live operator views preserving the latest recovery decision and checkpoint summary across attempt rollover after reacquire
10. the governed turn-tool path now publishes per-turn run and attempt authority, an issue-scoped namespace reservation on governed admission with explicit promotion to an active execution lease on execution start, shared `ResourceRecord` snapshots on namespace activation, closeout release, and fail-closed activation rollback, governed protocol receipt invocation manifests that now carry the canonical governed run, attempt, reservation, lease, and namespace-resource ids when that durable authority exists, pre-effect `resume_same_attempt` checkpoints backed by immutable checkpoint snapshot artifacts whose acceptance now depends explicitly on that reservation/lease pair, checkpoint-backed same-attempt recovery decisions for unfinished pre-effect `resume_mode` recovery that now consume the accepted checkpoint snapshot before prompt/model work and continue on the current attempt while still consuming older governed new-attempt lineage truthfully when it exists, reconciliation-record publication plus immediate reconciliation-closed terminal closeout once unfinished post-effect, effect-boundary-uncertain, already-dirty same-attempt or replacement-attempt truth, or orphan durable operation artifacts exist, per-operation step records, effect-journal entries, terminal recovery decisions with checkpoint/effect stop basis plus final truth for blocked or failed governed execution, reuses durable completed-run step, effect, and operation artifacts before prompt/model work and before checkpoint artifact rewrite on successful governed re-entry only when accepted checkpoint authority, immutable snapshot identity, and durable operation alignment still hold with no unexpected operation artifacts present, now also fails closed on existing-run reuse when the latest namespace resource snapshot no longer agrees with the active or completed lease authority, fails closed before model invocation and checkpoint artifact rewrite when the governed run is already terminal or recovery-blocked, and uses an explicit default `issue:<issue_id>` namespace with fail-closed namespace targeting on the default execution pipeline path across protocol and non-protocol turns
11. the Gitea state worker path now publishes one lease-backed non-sandbox run per claimed card and lease epoch, a first-class claim reservation after backend lease acquire with promotion only after the initial claim transition succeeds and invalidation on claim-stage failure, one pre-effect `resume_forbidden` checkpoint from the claimed-card observation, one attempt, worker-owned claim/finalize steps plus a failed claim step when the initial claim mutation fails, effect-journal entries for observed state transitions, claim-scope reconciliation on failed initial claim mutations, terminal recovery decisions for lease-expiry, claim-transition failure, and runtime failure, terminal final truth, first-class lease history on acquire, renew, expiry, release, and claim-failure uncertainty, shared `ResourceRecord` history on those same ownership transitions including promotion rollback, and terminal expiry/release helpers now realign resource truth if a latest resource snapshot drifted away from the already-terminal lease
12. the standalone coordinator API now publishes first-class non-hedged claim reservations with explicit promotion to lease authority, first-class lease history for claim, renew, expiry-before-reclaim, open-cards expiry observation, and release transitions on the default coordinator surface, shared `ResourceRecord` history across those same transitions, fail-closed promotion rollback that now mirrors the released lease through the same shared resource seam, and fail-closed coordinator renew/expiry/release guards plus open-cards/claim stale-expiry preflight when the latest shared resource snapshot no longer agrees with lease authority, while its list/claim/renew/complete/fail responses expose the latest reservation, lease, and resource summary including reservation kind, promotion rule, lease resource id, lease expiry basis, granted timestamp, publication timestamp, last confirmed observation, and resource current state
13. the sandbox create-record failure path now fail-closes partial allocation authority by releasing any just-published create-path lease, invalidating the still-unpromoted reservation, and publishing a matching shared `ResourceRecord` closeout even when durable lifecycle authority never finished initializing
14. default orchestrator scheduler-owned issue transitions that happen without an active dispatch run now publish namespace-scoped reservation, lease, shared `ResourceRecord`, step, effect-journal, and final-truth authority for dependency-block propagation and pre-dispatch retry or terminal issue mutations, team-replan child issue creation now publishes the same authority family for the child workload-composition mutation, and reused-run guards now fail closed when the latest resource snapshot disagrees with the closed namespace lease
15. default orchestrator issue-dispatch now publishes a shared issue-dispatch-slot `ResourceRecord` alongside dispatch-slot lease activation, terminal closeout release, and fail-closed begin-dispatch rollback on the same control-plane authority seam that already owns its reservation, lease, step, effect, and final-truth publication, and active/closed dispatch guardrails now fail closed when the latest resource snapshot disagrees with the lease
16. control-plane contract, journal, recovery, reservation, lease, run, attempt, step, checkpoint, final-truth, and shared resource publication seams in core and application services

The weakest areas, and therefore the primary implementation risks, are:
1. reservation truth now covers sandbox allocation, approval-gated operator holds for repo-backed tool approvals, guard-review pending gates, governed kernel approvals, default orchestrator issue dispatch, default orchestrator scheduler-owned issue transitions and team-replan child issue composition, standalone coordinator claims, and Gitea worker claim admission, but it is still not wired into all admission and scheduling paths
2. lease and resource truth are no longer sandbox-only, and the standalone coordinator API, Gitea worker, governed kernel-action path, governed turn-tool path, default orchestrator issue-dispatch path, and default orchestrator scheduler-owned issue mutation path now publish shared non-sandbox ownership history, with the coordinator renew/expiry/release path, Gitea worker active renew path, issue-dispatch, scheduler, governed turn-tool, and governed kernel existing-run guards now also failing closed on resource-versus-lease drift while Gitea worker terminal expiry/release helpers now realign resource truth instead of trusting lease-only terminal history, and governed turn-tool protocol receipts now project canonical run/attempt/reservation/lease/resource ids for that path, but that truth is still not projected consistently across broader read surfaces or every admission and scheduling path
3. operator-action authority is still fragmented in live runtime behavior outside explicit authenticated sandbox stop, authenticated session halt, authenticated interaction cancel, approval-resolution publication, and authenticated kernel session-end command/attestation publication
4. effect truth is still often reconstructed from artifacts outside the sandbox, governed kernel action, default orchestrator issue-dispatch plus scheduler-owned issue mutation and team-replan child issue composition, and default turn-tool paths instead of being published through the normative effect journal
5. checkpoint publication is now live on the sandbox reclaimable path, the governed turn execution pre-effect path, and the Gitea worker pre-effect path, checkpoint-backed new-attempt recovery is now live on the sandbox reclaimable path, checkpoint-backed same-attempt recovery is now live on unfinished pre-effect governed turn `resume_mode`, and governed-turn replay still consumes older new-attempt lineage truthfully when it exists, but broader supervisor-owned checkpoint creation still is not execution-default behavior
6. namespace and safe-tooling gates are now live on the default turn-tool path across both protocol and non-protocol turns, and the default orchestrator scheduler-owned issue mutation and team-replan child issue composition paths now publish explicit issue-scoped namespace authority, but broader runtime workloads, scheduling, and resource targeting still do not share one explicit namespace authority surface

## Planning rule

The implementation lane must use this crosswalk to:
1. name which current surfaces are being promoted
2. name which current surfaces are being superseded
3. name which packet noun is still missing in code

If a planned code slice cannot point to a row in this crosswalk, the slice is not specific enough yet.
