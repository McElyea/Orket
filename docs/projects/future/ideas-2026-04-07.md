Here's what I'd build around the Card Viewer/Runner as the anchor:
Tab 1 — Cards (your starting point)
The main work surface. Left sidebar lists cards filterable by state (queued, running, failed, done), type, and tier. Right panel is the card detail — prompt, last run output, run history, and a big "Run" button. Live streaming output when a card is running. This is the "cockpit" — the thing you look at most.
Tab 2 — Run History / Ledger
A chronological feed of every run: card ID, model, duration, pass/fail, cost, determinism hash. Filterable. Click a row to expand full output, artifacts, and the canonical hash comparison. This surfaces what's currently buried in benchmarks/results/ JSON files.
Tab 3 — Benchmark / Quant
The sweep and scoring view. Run a quant sweep, watch it progress, see pass/fail by tier, score distribution, and regressions highlighted in red. Right now this is entirely CLI and output files — a UI here would be genuinely transformative for your workflow.
Tab 4 — Model Health Matrix
Provider/model health matrix. Connect to Ollama or LMStudio, run the provider runtime matrix, see which rig classes pass per model. Toggle a model active/inactive. This is a natural UI because the matrix output already has a table shape.
Tab 5 — System / Settings
Gitea connection status, sandbox lifecycle health, spool status (how many events are backed up), active API keys, and the control plane convergence status summary. One place to see if anything is on fire.
The key design principle I'd push for: every tab should have a status indicator in the tab itself (a green dot, a count badge, a spinner). Cards shouldn't just be a list — tabs 2 and 3 should tell you something is wrong before you even click them.


-----

FTS5/vector memory store — replace MemoryStore with SQLite FTS5 first, then add an embedding sidecar (chromadb or hnswlib) once you need semantic retrieval
Shared rate limiter via Redis — prerequisite for safe multi-worker deployment
Token revocation store — Redis-backed JWT blocklist with TTL matching token lifetime
Unified session registry — merge GlobalState + InteractionManager (your biggest architectural debt)
Streaming budget negotiation — per-workload token delta budgets so model runs don't get silently truncated
Distributed tracing (OpenTelemetry) — the codebase has rich logging events but no trace correlation; add span propagation across agent → tool → storage
Async Agent.create() factory — replace constructor-blocking file I/O with proper async init
Structured error hierarchy — replace stringly-typed ValueError("error_code") with a real exception tree
Connection pool for Gitea HTTP — prerequisite for any Gitea-heavy scaling
Webhook idempotency store — replay protection beyond the current in-memory deque
Tool call result schema validation — structured validation layer between tool output and agent consumption
Review policy DSL hardening — replace the password\s*= false-positive patterns with a tested, domain-specific ruleset
Agent prompt injection hardening — structured delimiters + context escaping (formal solution to P1-5)
Multi-tenant session isolation — explicit org/tenant boundaries in session and card ownership
Gitea snapshot size budgeting — body size enforcement before PATCH to prevent silent 422s
Sandbox service allow-list via config — move _allowed_log_services to org config
ULID library extraction — your custom ULID generator is good; extract it to a shared utility module with proper tests
Atomic card archive operations — multi-card archive should be a DB transaction, not a loop
Commit artifact integrity verification — SHA-256 manifest file alongside each authority_commit.json so consumers can detect corruption
Streaming bus metrics — emit STREAM_TRUNCATED counts and budget exhaustion metrics to your observability stack
Protocol ledger circuit breaker — escalate parity failures to alerts instead of silent telemetry
Review lane retry and classification — distinguish transient vs. permanent model provider errors
Sprint schedule configuration — externalize the hardcoded 2026-02-02 base date to a config file
WebSocket auth middleware — explicit auth token validation on WebSocket upgrade before session creation
End-to-end streaming integration test — one test that drives model_stream_v1 through a full turn cycle against the stub provider and asserts no events are dropped, all lifecycle events fire in order, and commit is issued correctly