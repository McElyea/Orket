{
  "slice_id": "CORE-IMP-02",
  "status": "in_progress",
  "generated_on": "2026-03-06",
  "evidence": {
    "test_command": "python -m pytest tests/runtime/test_protocol_replay.py tests/runtime/test_protocol_determinism_campaign.py tests/scripts/test_run_protocol_replay_compare.py tests/scripts/test_run_protocol_determinism_campaign.py -q",
    "result": "pass",
    "passed": 20,
    "failed": 0
  },
  "implemented_now": [
    "protocol replay summary now emits runtime_contract_hash derived from runtime contract snapshot versions/hashes",
    "runtime_policy_versions snapshot is now emitted in replay summaries and included in runtime_contract_hash inputs",
    "protocol replay now resolves and enforces single compatible ledger_schema_version and fails closed on incompatible/mixed schema versions",
    "protocol replay comparison now surfaces ledger_schema_version and runtime_contract_hash drift as first-class deterministic differences",
    "new contract test covers replay fail-closed behavior for incompatible ledger schema versions"
  ],
  "pending_for_slice_completion": [
    "replay-mode isolation guards to ensure no inference/prompt-construction/repair paths execute during replay",
    "fail-closed compatibility checks for tool registry snapshot, artifact schema registry version, capability profile snapshot, and workspace snapshot",
    "replay completeness validation for required artifacts before execution",
    "drift classifier deterministic precedence ordering and drift_schema_versioned drift report output"
  ]
}
